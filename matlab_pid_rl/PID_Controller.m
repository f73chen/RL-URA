function [gain] = PID_Controller(state, reward)
    % Actions: increase or decrease gain by 5%
    % Parameters:
    %   state: max absolute error for each phase during the previous cycle
    %   reward: deviation from reference trajectory in the previous cycle

end

